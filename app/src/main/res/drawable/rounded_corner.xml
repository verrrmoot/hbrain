<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="10dp" /> <!-- Задайте радиус скругления в dp -->
    <stroke
        android:width = "2dp"
        android:color="@color/orange"
        />
    <solid android:color="@android:color/transparent" /> <!-- Цвет заливки (в данном случае прозрачный) -->
</shape>
