<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="20dp" /> <!-- Задайте радиус скругления в dp -->
    <stroke
        android:width = "1dp"
        android:color="@color/divider_color"
        />
    <solid android:color="@android:color/transparent" /> <!-- Цвет заливки (в данном случае прозрачный) -->
</shape>
