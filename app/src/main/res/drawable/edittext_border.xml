<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width = "4dp"
        android:color="@color/divider_color"
    />
    <corners android:radius="8dp" /> <!-- Опционально, для скругления углов -->
</shape>
